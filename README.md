# Kaggle LLM Project

Google Colabにリモート接続して開発するためのワークフローガイド。

## 🚀 クイックスタート

### 1. Colab側の設定（毎回）

Colabノートブックで以下を実行：

```python
!pip install colab-ssh
from colab_ssh import launch_ssh_cloudflared
launch_ssh_cloudflared(password="your_password")
```

表示されるホスト名（`xxx.trycloudflare.com`）をメモ。

### 2. ローカルから接続

```bash
ssh <ホスト名>.trycloudflare.com
# パスワード入力
```

---

## 📁 ファイル転送方法

### 方法1: ヒアドキュメント（推奨）

**単一ファイル：**
```bash
# SSH接続中のColabで実行
cat > /root/ファイル名.py << 'EOF'
（ファイル内容をペースト）
EOF
```

**クリップボード経由（ローカルで）：**
```bash
cat /path/to/file.py | pbcopy
```
### 方法2: Git経由

```bash
# ローカル
git add . && git commit -m "update" && git push

# Colab
git clone https://github.com/user/repo.git
# or
git pull
```

---

## 📦 ライブラリインストール

```bash
# SSH接続中のColabで
pip install ライブラリ名

# requirements.txtから
pip install -r requirements.txt
```

---

## 📂 ファイルの場所

| 場所 | パス | 用途 |
|-----|------|------|
| SSHホーム | `/root/` | SSH経由で作成したファイル |
| Colabデフォルト | `/content/` | ノートブックのファイル |
| Google Drive | `/content/drive/MyDrive/` | 永続ストレージ |

---

## ⚙️ 初期設定（一度だけ）

### cloudflaredインストール

```bash
brew install cloudflared
```

### SSH config設定

`~/.ssh/config` に追加：

```
Host *.trycloudflare.com
    HostName %h
    User root
    Port 22
    ProxyCommand /usr/local/bin/cloudflared access ssh --hostname %h
```

---

## 💡 Tips

- **セッション切れ**: Colabが切れるとホスト名が変わる。再接続時は新しいホスト名を使用
- **GPU有効化**: Colab → ランタイム → ランタイムのタイプを変更 → GPU
- **永続化**: 重要なファイルはGoogle Driveに保存

---

## 🔧 トラブルシューティング

| 問題 | 解決策 |
|-----|--------|
| rsync/scpが動かない | ヒアドキュメント方式を使用 |
| nvidia-smiがない | ColabでGPUランタイムを選択 |
| 接続タイムアウト | Colabセッションが切れている可能性 |

---

## 📝 ワークフロー

```
1. Antigravity でローカルにコード作成
   ↓
2. ヒアドキュメント/Git で Colab に転送
   ↓
3. Colab で実行（GPU利用）
   ↓
4. 結果をローカルに戻す（必要に応じて）
```
